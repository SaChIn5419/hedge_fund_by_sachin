# Regime Models

This directory is reserved for probabilistic regime-detection models, transition models, and calibration artifacts.

Suggested split:
- `features/`: regime-specific feature definitions
- `states/`: state classifiers or HMM artifacts
- `transitions/`: next-state models and calibration outputs

